## 目标
- 在任务管理页支持任务编辑：可修改标题、描述、分类、优先级、预计时长与截止日期。

## 最小改动策略
- 直接在 `TaskManager.tsx` 中实现编辑弹层，复用现有 `editingTask` 状态。
- 使用本地状态承载表单值；保存时更新 `tasks` 数组并关闭弹层。

## 实施步骤
1. 在 `TaskManager.tsx` 增加表单本地状态（title/description/category/priority/estimatedDuration/dueDateStr）。
2. 当 `editingTask` 变化时，用 `useEffect` 将其值填充到表单状态。
3. 编辑弹层 UI：覆盖层 +卡片 + 输入/选择控件 + 取消/保存按钮；移动端底部抽屉、桌面居中。
4. 保存逻辑：校验标题非空，序列化 `dueDateStr` 为 `Date`，更新目标任务并关闭弹层。

## 修改文件
- `src/components/tasks/TaskManager.tsx`
  - 增加本地状态与 `useEffect`
  - 增加编辑弹层 UI 与保存处理函数

## 验证
- 在任务卡片点击编辑图标，弹层出现，修改后保存，列表即时更新。
- 空标题提示或禁止保存（基础校验）。

确认后我将按上述步骤实施并完成验证。