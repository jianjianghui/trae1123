## 目标
- 将桌面左侧标签栏在移动端改为底部标签栏。
- 三个主标签（自左向右）：智能任务管理、AI对话、个人中心。
- 将“番茄钟”整合进任务管理页中，而非作为独立标签。
- 保持桌面端现有布局不变，移动端进行响应式适配。

## 最小改动策略
- 不引入新路由，继续沿用 `App.tsx` 的 `activeTab` 切换（速度更快，改动集中）。
- 移动端隐藏左侧侧栏（`hidden md:block`），新增底部标签栏（`md:hidden`）。
- AI对话暂用现有 `AITaskParser` 组件作为对话入口与展示，标签文字统一为“AI对话”。
- 个人中心先提供轻量页面，显示用户 MBTI 类型及占位信息，满足演示需求。

## 具体实施步骤
1. 移动端底部标签栏
   - 在 `src/App.tsx` 主返回块的内容区末尾（靠近 `Toaster` 之前，参考 118–121）新增一个底部标签栏容器，设置 `fixed bottom-0 left-0 right-0 md:hidden`，三项：`tasks`（智能任务管理）、`parser`（AI对话）、`profile`（个人中心）。
   - 点击各项时更新 `activeTab`（沿用现状按钮的 `setActiveTab('...')`）。
   - 将左侧侧栏整体加上 `hidden md:block`，使其仅桌面显示。

2. 标签与内容映射
   - `activeTab === 'tasks'` → 渲染 `<TaskManager userMBTI={mbtiResult!.type} />`。
   - `activeTab === 'parser'` → 渲染 `<AITaskParser userMBTI={mbtiResult!.type} onTaskParsed={handleTaskParsed} />`，标签文字显示“AI对话”。
   - 新增 `activeTab === 'profile'` → 渲染新建的 `Profile` 页面组件（轻量）。
   - 移除移动端直接进入 `timer` 的入口；桌面侧栏可保留“个性化番茄钟”按钮（演示时主要强调移动端）。

3. 番茄钟整合到任务管理页
   - 在 `src/components/tasks/TaskManager.tsx` 顶部引入 `PomodoroTimer`。
   - 在页面主内容区的标题与操作区之下（约 175–190 后），嵌入 `<PomodoroTimer userMBTI={userMBTI} />` 并提供一个简易展开/收起开关，默认展开用于演示。
   - 维持现有过滤器和任务列表结构不变。

4. 个人中心页面（轻量）
   - 新建 `src/pages/Profile.tsx`，展示：
     - 用户类型：`mbtiResult?.type`。
     - 简短说明与占位项（如“偏好、通知设置、使用统计”占位）。
   - 在 `App.tsx` 中引入并在 `activeTab === 'profile'` 时渲染。

5. 文案与图标
   - 侧栏与底部标签栏的按钮文字统一：
     - `tasks`: “智能任务管理”
     - `parser`: “AI对话”
     - `profile`: “个人中心”
   - 保持顶部导航现有标题与用户徽标不变。

## 修改文件与关键位置
- `src/App.tsx`
  - 顶部导航：约 26–46；无需更改。
  - 左侧侧栏：49–108（其中按钮行：70、82、94）→ 加 `hidden md:block` 并统一文案。
  - 内容区渲染：111–116 → 新增 `profile` 分支；保留 `tasks`、`parser`；`timer` 移动端不再露出。
  - 底部标签栏：在返回块末尾、靠近 `Toaster`（118–121）前后新增移动端专用容器。
- `src/components/tasks/TaskManager.tsx`
  - 标题/操作区：175–190 → 在其后插入 `PomodoroTimer` 模块（带展开/收起）。
- `src/components/tasks/AITaskParser.tsx`
  - 组件维持不变，作为“AI对话”展示使用。
- 新增：`src/pages/Profile.tsx`
  - 轻量展示 MBTI 类型与占位内容。

## 验证清单
- 切换至移动端视口，左侧侧栏隐藏、底部标签栏显示且可点击切换三页。
- “智能任务管理”页顶部显示番茄钟，计时可开始/暂停，工作/休息切换正常。
- “AI对话”页可输入并触发解析，结果展示正常（沿用现有解析组件）。
- “个人中心”页显示当前用户类型（来源于 `mbtiResult`），页面正常渲染。
- 桌面端保持现有三段式布局与交互。

## 备选与权衡（如需）
- 若时间允许，可将 `activeTab` 切换升级为 `react-router-dom` 路由（在 `src/main.tsx` 包 `BrowserRouter`，在 `App.tsx` 用 `Routes/Route`），但为保证 1 小时内演示，当前方案采用最小改动与最快落地。

如确认该方案，我将按上述步骤实施，并确保在演示前完成本地验证。